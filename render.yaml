services:
  - type: web
    name: banking-analysis
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: GROQ_API_KEY_BK
        sync: false
      - key: PORT
        value: 10000
